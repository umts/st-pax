%h1 Edit Passenger Registration

- if @current_user || @registrant&.pending?
  = render partial: 'form', locals: { passenger: @passenger }
- else
  Your registration is #{@passenger.active_status}. To edit your profile,
  please call (413) 545-2086

%p
  = link_to 'Back', :back
